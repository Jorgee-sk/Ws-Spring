package service;

import java.util.List;

import model.Alumno;

public interface BuscadorService {
	List <Alumno> buscar(String seccion);
	void alta(Alumno p);
	void eliminar(String nombre);
	void modificar(String nombre, double p);
	Alumno buscarProducto(int idProducto);
	
}
